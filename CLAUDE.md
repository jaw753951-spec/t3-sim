# INTERN 전투 시뮬레이터

## 이 저장소를 만지는 법

`intern_sim.html` 은 **결과물이다. 직접 고치지 않는다.** 고칠 곳은 `src/` 아래고
`node build.js` 가 한 장으로 이어 붙인다. 층이 무엇을 맡는지는 `docs/구조.md` 에 있다.

```
node build.js                       src/ → intern_sim.html
node build.js --check               뽑지 않고 순서 · 중복 · 문법만 본다
node sim_check.js                   불변 조건 (레벨표 · 카드 본문 · 팩 · 병 비트)
node sim_check.js  <옛것> <새것>     판정을 견준다 — 「판은 같다」가 나와야 한다
node ui_check.js   <옛것> <새것>     화면을 견준다 (헤드리스 크로미움)
node story_probe.js sweep 아이,어부,송이 [파일]    병이 노는 구간
node story_probe.js diff <옛것> <새것>             스토리 360판을 견준다
node stage_check.js [파일]           무대 자리 재기 (구역 겹침 · 칩 합 · 설명 빠짐)
```

**규칙을 안 건드린 변경이면 `sim_check` 견주기가 「판은 같다」여야 한다.** 달라졌다면
의도한 것인지 먼저 대답할 수 있어야 한다.

### 검사기를 돌릴 때 걸리는 것

- `story_probe.js sweep` 은 **파일이 두 번째 인자**다 (`sweep <보스목록> <파일>`).
  첫 인자에 파일을 주면 보스 이름으로 읽어 `stage0` 에서 튄다.
- 헤드리스로 판을 세워 볼 때 번들의 `S` 는 스크립트 스코프 `let` 이라
  **`window.S` 로는 못 닿는다.** `eval` 로 그 자리에 넣어야 한다. 안 그러면
  페이지가 원래 들고 있던 판을 검사하게 되고, 그걸 모르면 결과를 거꾸로 읽는다.
- **`sim_check` 와 `ui_check` 는 자리를 못 본다.** 둘은 판정과 글자를 견주므로
  칸이 서로 겹치거나 카드가 손패 줄 밖으로 나가도 글자는 그대로라 통과한다.
  화면 배치를 건드렸으면 `stage_check.js` 를 같이 돌린다 — 눈으로 넘기다
  실제로 놓쳤던 것들(차트와 나가기 버튼이 겹침 · 카드가 줄 위로 27px 삐져나옴 ·
  병 노드 배지가 테에서 127px 밖)이 전부 여기서 걸렸다.

---

## 주석 규약 — 이것만은 지킨다

`src/` 의 주석은 이 저장소에서 값이 제일 큰 물건이다. 버그를 찾을 때 실제로 읽고
쓴다. **고칠 때마다 같이 갱신한다.**

### 적을 것 둘

**① 이 값이 왜 이 값인가.** 손잡이 옆에는 숫자만 두지 말고 그 숫자를 고른 근거를 둔다.

> 광역 억제 계수 1/3 은 밸런스 손잡이가 아니다. 0~2/3 사이에서 중앙 턴이 0~4턴
> 움직이고 사망률은 거의 안 움직인다. 도미노 폭주는 안 일어났다.
> — `src/30-core/kernel.js`

**② 전에 어떻게 틀렸는가.** 고친 버그 자리에는 증상 · 원인 · 무엇이 그것을 드러냈는지를
남긴다. **이 종류가 압도적으로 유용하다** — 코드만 봐서는 절대 못 얻는다.

> 배선 목록에 '끊은 자리'를 도로 넣어야 한다. … v20 까지 기본형 촉발 · 전이가
> 한 번도 터지지 않은 원인이다. — `src/30-core/kernel.js` `fireReactions`

실제로 이 주석 하나가 무대 쪽(`stage.js`)이 **같은 함정에 빠져 있는 것**을 알아보게
했다. 커널은 v20 에 고쳤는데 화면이 그 계산을 따로 흉내 내다 같은 데서 틀리고 있었다.

### 곁들여 적을 것

- **걷어냈으면 왜 걷었는지.** (`RUSH_SCOPE` · 카드 축 ④ `when` 이 본보기)
  걷었다는 사실만 남기면 다음 사람이 다시 넣는다.
- **두 곳이 같은 값을 쓰면 갈렸을 때 무슨 일이 나는지.**
  > 화면 예고와 실제 억제가 같은 함수를 쓴다 — 여기가 갈리면 카드가 거짓말을 하게 된다.
  > — `src/30-core/kernel.js` `supAmt`
- **되살릴 때의 함정.** 죽은 코드를 걷을 때, 되살리려면 무엇을 같이 챙겨야 하는지.
  (「출력 개방」의 `raw` 자리가 본보기 — 개방은 보호막을 무시하므로 `{raw:true}` 가 필요하다)
- **차례가 곧 규칙인 자리는 차례를 적는다.** (`turnResolve` 의 턴 해결 순서)

### 적지 말 것

- 코드를 그대로 옮겨 적은 설명. 「무엇을 한다」는 코드가 이미 말한다.
- `intern_sim.html` 에 손으로 적은 주석. 결과물이라 다음 빌드에 날아간다.

### 값을 글자로 박지 않는다

문안이 규칙값을 말할 때는 **손잡이에서 읽어 온다.** 덮어쓰기 칸(`80-tools/override.js`)이
`R` · `SR` 을 바꾸므로, 글자로 박아 두면 그 순간부터 그 설명만 옛 값을 말한다.
`policyTip` 의 「50% · 20% · 1.5 − 0.6 = 0.9」와 처치선 설명의 「5%p」가 실제로 그랬다.

---

## 층 규칙

- `10-config` ~ `40-ai` 는 **화면을 한 번도 쓰지 않는다.** `sim_check` 가 이 구간을
  `//@ 자름.*` 앵커로 떼어 node 에서 돌린다 — 그 앵커는 부품이지 주석이 아니다.
- 커널이 `S.ev` 에 사건을 쌓는 것은 **훅이 아니라 자료**다. 화면을 부르지 않고 배열에
  밀어 넣기만 하므로 층 규칙을 어기지 않는다. `S.ev` 를 안 켜면 아무것도 안 쌓인다.
- 앞의 층은 뒤의 층을 모른다. `50-wire.js` 가 참조를 잇는다.
