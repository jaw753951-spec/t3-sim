/* ══════════════════════════════════════════════════════════════════
   판본
   ──────────────────────────────────────────────────────────────────
   판본은 여기 한 줄이 유일한 자리다.
   제목 · 왼쪽 위 이름표 · 차트 머리가 전부 이 값을 쓴다 —
   껍데기(HTML)는 {{VERSION}} 이라 적어 두면 build.js 가 갈아 넣는다.
   ══════════════════════════════════════════════════════════════════ */
//@ 판본 — 고치는 곳은 여기 하나
const VERSION = '0.2';
