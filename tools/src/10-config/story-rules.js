/* ══════════════════════════════════════════════════════════════════
   §6 스토리 전용 수치 손잡이
   원본 intern_sim_v25.html 에서 그대로 옮겨 왔다. 내용 변경 없음.
   ══════════════════════════════════════════════════════════════════ */
/* ═══════════════════════════════════════════════════════════════
   스토리 전투 v19 — 단계 6-2
   1막 = 병명을 밝힌다 (병 노드에 손 못 댐)
   2막 = 문진 · 치료 방침을 고른다
   3막 = 병 노드와 싸운다. 방침마다 다른 판이 된다.
   출처: 시스템기획_정리 「스토리 전투」·「전투 결과」

   ── 연명 ⒜ 결정 (2026-08-15 우산) ──────────────────────────
   연명은 플레이어가 고르는 출구가 아니라 판이 강제하는 결과다.
   핵심을 처치할 수 없는 상황에서 휴면을 걸고 마무리하는 것.
   §4.5 손익분기표의 "전 노드 휴면 = 30~40% 절약"은 폐기 대상 —
   처치선 50% 아래에서 재우기는 뽑기보다 두 배 비싸다.
   ═══════════════════════════════════════════════════════════════ */

/* ── 스토리 상수 ── */
//@ 스토리.SR — 스토리 전용 수치 손잡이
const SR = {
  /* 병 노드 처치선은 R 이 유일한 자리다 — R.DIS_KILL_LINE */
  /* ★ 문서 공백: 병 노드 처치선이 0이면 연명과 완치가 1코스트 차이로 붙는다.
     연명이 따로 성립하려면 별도의 '재우는 선'이 필요하다. 잠정 30%. */

  /* 검증 8번(병 노드 스케일)은 열린 채로 둔다.
     이 AI 기준으로는 원안에서 완치 0%였으나, 사람은 원안으로 완치한다(정연).
     즉 이 시뮬의 완치율은 하한선이지 설계값이 아니다. */
  /* v25 — 아래 둘은 레벨표에서 뽑아 쓴다. 손으로 적던 표를 지웠다.
     보스는 항목마다 어느 레벨을 보는지만 정한다 (BOSS[].lv). */
  DIS_BASE: Object.fromEntries(Object.entries(LVTAB).map(([k,v])=>[k, v.dis])),
  STAGE_TURNS: 6,                    // 원안 유지
  SPAWN_LV: Object.fromEntries(Object.entries(LVTAB).map(([k,v])=>[k, v.n[1]])),

  /* 방침 디버프 — disCut·painCut 은 정진단일 때만 붙는다 */
  POLICY: {
    완치:   {disCut:0.40, stageBonus:0, painCut:0,    tag:'마주'},
    연명:   {disCut:0.20, stageBonus:2, painCut:0,    tag:'타협'},
    편하게: {disCut:0,    stageBonus:3, painCut:0.30, dmgUp:0.50, tag:'도피'},
  },
  /* 「편하게」 승리 조건 — 병이 최종 병기의 시계를 다 돌 때까지 환자가 살아 있으면 이긴다.
     완화 폭은 R.COMFORT_CUT 하나로 관리한다. */

  /* 박자가 자리를 얼마나 움직이는가 — 그 자리 **현재 수치**에 대한 비율.
     전부 VAL_CAP(초기값 기준 천장)에서 잘린다. 「가라앉는다」만 빼는 쪽이고 나머지는 얹는 쪽이다.

     ★ 초기값 기준이던 것을 현재값 기준으로 옮겼다. 화면에 뜬 숫자에 비율만 걸면
       되도록 하는 것이 목적이다 — 초기값 기준일 때는 깎아 놓은 자리도 늘 같은 값이
       얹혀서, 판을 보고 다음 턴을 셈할 수가 없었다 (지연 규칙을 옮긴 것과 같은 결이다).

     성장만 값을 내린 까닭 — 성장은 **폴백이라 매 턴 붙을 수 있다.** 현재값 기준으로
     옮기면 복리가 되므로, 억제율 아래에 두는 기존 제약을 지키려고 0.15 → 0.07 로
     내렸다. 나머지 셋은 악보에 박힌 자리에서만 나오므로 연쇄가 불가능하고 제약이 안 걸린다.
     「몰린다」는 여기서 빠졌다 — 비율이 아니라 병 노드 기준값의 절대 몫이 됐다 (BEAT_CROWD). */
  BEAT: {
    성장:       0.07,   // 성장 비트 — 살아 있는 부수 자리 전부. 폴백으로도 쓴다
    번진다:     0.25,   // 어부 — 통증만 골라서
    치민다:     0.30,   // 송이 — 금단의 정점. 전부
    가라앉는다: 0.25,   // 송이 — 내려가는 폭. 1 아래로는 안 간다
  },

  /* 「몰린다」 — 가장 옅은 활성 자리 하나를 이만큼 올린다. **병 노드 기준값의 비율**이고
     자리의 초기값도 현재값도 보지 않는다. 기준값은 레벨표의 병 노드 수치 열(stageDisVal)이라
     완치가 병 노드를 깎아도 이 값이 안 바뀐다 — 병기 3·4·5 에서 12 · 18 · 25 다.
     절대값이라 비율 계산이 없다: 옅은 자리일수록 크게 되살아난다. */
  BEAT_CROWD:    0.10,

  BEAT_SCRATCH_N: 2,  // 「긁는다」 — 활성 통증 자리가 이만큼 이상이어야 이차 감염이 앉는다

  /* 자리를 세울 때의 초기값 — 밑값에 폭만큼 난수를 얹는다 */
  SPOT_SPREAD:      15,   // 명부대로 세울 때, 명부가 준 밴드 위에 얹는 폭
  ROSTER_MISS:      50,   // 명부에 그 증상이 없을 때 대신 쓰는 밴드
  FREE_BASE:        40,   // 명부가 없는 보스 — 밑값
  FREE_SPREAD:      20,   //  그 폭
  DUP_BASE:         35,   // 분화로 새로 나는 자리 — 밑값
  DUP_SPREAD:       25,   //  그 폭

  /* 자동 진행이 한 막에 쓸 수 있는 턴 */
  ACT1_CAP:  12,
  ACT3_CAP:  30,

  /* 1막 — 검사 파라미터 */
  PARAM_NEED: 4,                     // 파라미터 하나를 채우는 데 드는 진단 스택
  EVID_TOTAL: 4,                     // 병마다 증거 넷
  EVID_AIM: 3,                       // 자동 진행이 여기까지 모으면 멈춘다. 넷째는 손으로 더 캘 수 있다
  CAND_BASE: 6,                      // 차트에 처음 뜨는 후보 병명 수
  ACT1_SPAWN_EVERY: 2,               // 1막에서 병 노드가 부수 증상을 뿜는 주기

  /* ── 최종 병기 — 앵커와 이음표 ────────────────────────────────
     앞 병기는 고정 악보이고 최종 병기만 이 둘로 돈다.
     앵커  = 보스 고유 기믹이 ANCHOR_EVERY 턴 주기로 온다. 첫 칸이 앵커다.
     이음표 = 앵커 사이 칸을 채운다. 직전 행동을 키로 후보를 얻어 위에서부터 조건을 본다.
     ANCHOR_LOOPS 바퀴까지만 앵커가 발동하고 그 뒤로는 성장으로 대체된다 —
     안 두면 최종 병기가 길어질수록 고유 기믹만 반복하는 판이 된다. */
  ANCHOR_EVERY:  4,
  ANCHOR_LOOPS:  5,

  /* 성장이 이만큼 연달아 나오면 다음 한 칸은 앵커로 넘긴다.
     판이 좁아졌을 때(자리가 다 죽어 대상 없는 박자가 줄줄이 폴백으로 새는 구간)
     병이 헛도는 것을 막는다 — 성장은 폴백이라 그 구간에서 매 턴 붙는다. */
  GROW_RUN_MAX:  2,

  /* ── 연명 전용 ──────────────────────────────────────────────
     처치한 자리는 닫히고 분화가 거기 다시 못 선다. 병기가 오를 때마다 정원이 이만큼 는다.
     휴면과 소멸은 자리를 **안 닫는다** — 연명 진척을 0으로 되돌리는 장치가 그 둘이다. */
  LINGER_SPAWN:  1,

  /* ── 고유 기믹의 흡수·회복 몫 ── 첫 값이라 조정 대상이다 ──── */
  WINDOW_DRAIN:  0.10,   // 창 — 소멸시킨 자리 수치 합의 이 비율을 병 노드가 회복한다
  MOVE_DRAIN:    0.50,   // 옮아 앉는다 — 굵은 자리 수치의 이 비율을 흡수한다. 나머지가 새 자리의 수치다
  CRAVE_HEAL:    0.50,   // 갈망 — 안정화로 깎은 보호막 값의 이 비율(올림)만큼 병 노드가 회복한다

  /* ── 창 기믹 손잡이 (2026-08-20) ── 절제 측정용으로 따로 꺼낸다 ── */
  GIMMICK: {
    WINDOW:  true,   // v25 — 창은 악보의 비트다. 끄면 「창」 비트가 무행동이 된다
    PRORATE: true,   // 병기 이월을 절대 → 비례로
    LOOP:    true,   // 최종 병기 도달 후 악보를 다시 돌린다
  },
};
